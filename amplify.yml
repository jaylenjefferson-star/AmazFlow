version: 1
applications:
  - appRoot: apps/web
    frontend:
      phases:
        preBuild:
          commands:
            - echo "Using verified AmazFlow static release artifact"
        build:
          commands:
            - test -f out/index.html
      artifacts:
        baseDirectory: out
        files:
          - '**/*'
      cache:
        paths: []
