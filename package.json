{
  "name": "amazflow-platform",
  "private": true,
  "packageManager": "pnpm@11.26.0",
  "scripts": {
    "dev:web": "pnpm --filter @amazflow/web dev",
    "dev:engine-sandbox": "pnpm --filter @amazflow/api dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "deploy:control-plane": "bash infrastructure/aws-cdk/scripts/deploy-control-plane.sh",
    "docs:page-classification": "tsx scripts/generate-page-classification.ts"
  },
  "devDependencies": {
    "@types/node": "^22.13.4",
    "tsx": "^4.19.3",
    "typescript": "^5.7.3"
  }
}
