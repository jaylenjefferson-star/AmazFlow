{
  "name": "@amazflow/browser-agent",
  "version": "0.8.0",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json && cp manifest.json popup.html assets/amazflow-icon.png dist/",
    "typecheck": "tsc -p tsconfig.json",
    "test": "node test/extension-contract.test.cjs",
    "release": "pnpm build && pnpm test && bash scripts/package-zip.sh"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.306",
    "typescript": "^5.7.3"
  }
}
