{
  "name": "@amazflow/browser-agent",
  "version": "0.8.0",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json && cp manifest.json popup.html dist/",
    "typecheck": "tsc -p tsconfig.json",
    "test": "node test/extension-contract.test.cjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.306",
    "typescript": "^5.7.3"
  }
}
