// Moved to `@amazflow/domain-ui` in task 9.2 -- requirement 3.2 asks for exactly ONE
// enumeration-to-label mapping in the repository, and this file plus `console/copy.ts` were two.
// The re-export keeps the twelve `/app` views importing `./terms` unchanged.
export * from "@amazflow/domain-ui";
